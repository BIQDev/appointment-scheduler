import { BiqMomentPipe } from './biq-moment.pipe';

describe('BiqMomentPipe', () => {
  it('create an instance', () => {
    const pipe = new BiqMomentPipe();
    expect(pipe).toBeTruthy();
  });
});
